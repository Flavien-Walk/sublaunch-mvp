// This file is intentionally empty — all test/mockup endpoints have been removed.
// Use the real flows: /api/saas/checkout, /api/stripe/create-checkout, /api/creator/profile
const express = require('express');
const router = express.Router();
module.exports = router;
